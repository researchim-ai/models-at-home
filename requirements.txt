# HomeLLM + Unsloth — совместимые версии
# =========================================================
# Python 3.12 + torch 2.9.x + flash-attn 2.8.3

# Triton (для Liger и Unsloth)
triton>=3.0.0

# ============================================================
# Core ML Stack (версии из Unsloth December-2025 pyproject.toml)
# ============================================================
# transformers: >=4.51.3, исключая баговые, <=4.57.3
transformers==4.57.1

# accelerate: trl 0.20+ требует >=1.4.0
accelerate>=1.4.0

# datasets: Unsloth требует ==4.3.0 (4.5.0 вызывает recursion errors)
datasets==4.3.0

# peft: Unsloth требует >=0.18.0
peft>=0.18.0

# trl: Unsloth требует >=0.18.2,!=0.19.0,<=0.24.0
trl>=0.18.2,!=0.19.0,<=0.24.0

# huggingface_hub
huggingface_hub>=0.34.0
hf_transfer
safetensors>=0.4.0

# ============================================================
# Unsloth — устанавливается отдельно в Dockerfile
# ============================================================
# (см. Dockerfile — ставится после основных пакетов)

# Liger Kernel
liger-kernel==0.7.0

# bitsandbytes для QLoRA
bitsandbytes>=0.45.0

# ============================================================
# Inference / Rollout
# ============================================================
# vLLM ставится отдельно в Dockerfile с --no-deps (чтобы не менять torch)

# ============================================================
# DeepSpeed (для ZeRO)
# ============================================================
deepspeed>=0.15.0
ninja

# ============================================================
# Утилиты
# ============================================================
tqdm>=4.66.0
numpy>=1.24.0
pandas>=2.0.0
psutil>=5.9.0
pydantic>=2.0
pyyaml>=6.0
tyro
sentencepiece>=0.2.0
packaging
protobuf

# UI
streamlit>=1.32.0
plotly>=5.18.0
graphviz>=0.20.1

# Логирование
wandb>=0.17.0
tensorboard>=2.16.0

# RL
# gymnasium>=1.0.0
# imageio>=2.34.0
# imageio-ffmpeg>=0.5.1
