import streamlit as st

def render_docs():
    """Отображает встроенную документацию."""
    
    st.header("📚 Учебник и Справка")
    st.markdown("Добро пожаловать в **Models at Home**! Здесь вы научитесь создавать свои нейросети с нуля.")

    # Создаем табы внутри документации для удобной навигации
    doc_tab1, doc_tab2, doc_tab3, doc_tab4 = st.tabs([
        "🚀 Пошаговая инструкция", 
        "🧠 Как это работает?", 
        "📖 Словарь терминов", 
        "💾 Подготовка данных"
    ])

    # --- 1. ПОШАГОВАЯ ИНСТРУКЦИЯ (Pretrain + SFT) ---
    with doc_tab1:
        st.markdown("""
        ### Как создать свою модель: Полный цикл

        Создание своей LLM состоит из двух главных этапов: **Pretraining** (Обучение с нуля) и **SFT** (Дообучение на инструкциях).
        
        ---

        #### 🟢 Шаг 1: Pretraining (Обучение с нуля)
        На этом этапе модель учится понимать язык, грамматику и факты о мире, читая гигабайты текста.

        **1. Скачивание данных**
        *   Перейдите на вкладку **💾 Данные**.
        *   В поле **"Репозиторий (ID)"** выберите `HuggingFaceFW/fineweb-2` (рекомендуем для русского языка выбрать subset `default` или `rus_Cyrl`, если есть).
        *   Нажмите **🔍 Проверить репозиторий**.
        *   **Важно:** Настройте лимиты, чтобы не качать весь интернет!
            *   В блоке **🛠️ Фильтры и Лимиты** выберите **"Тип лимита: Строки"**.
            *   Установите `100,000` строк для первой пробы (это ~100МБ текста).
            *   Или выберите **"ГБ (Размер)"** и укажите `1.0` ГБ.
        *   Нажмите **🚀 Скачать и обработать**. Файл `dataset.jsonl` появится в списке справа.

        **2. Настройка модели**
        *   Перейдите на вкладку **🚀 Запуск**.
        *   В сайдбаре (слева) найдите раздел **🧠 Архитектура и Режим**.
        *   **Этап обучения:** Выберите `Pretraining`.
        *   **Название эксперимента:** Придумайте имя, например `my_first_model`.
        *   **Пресет:** Выберите `Tiny (25M)` или `Small (80M)`. Для домашнего ПК это оптимально.

        **3. Запуск обучения**
        *   В разделе **⚙️ Параметры обучения**:
            *   **Batch Size:** 4 или 8.
            *   **Max Steps:** 1000 (для теста) или 10,000 (для результата).
        *   Убедитесь, что в поле **📁 Датасет** выбран ваш `dataset.jsonl`.
        *   Нажмите большую кнопку **▶️ Начать тренировку**.
        *   Следите за графиком **Loss** на вкладке **📊 Мониторинг**. Он должен падать 📉.

        ---

        #### 🔵 Шаг 2: SFT (Supervised Fine-Tuning)
        Теперь превратим "читателя" в "собезседника". Мы научим модель отвечать на вопросы.

        **1. Подготовка данных для SFT**
        *   Снова вкладка **💾 Данные**.
        *   В **"Популярные датасеты"** выберите `SFT: Alpaca (English)` или найдите русский аналог (например, `IlyaGusev/ru_turbo_alpaca`).
        *   Скачайте его так же, как в Шаге 1. Назовите файл, например, `sft_data.jsonl`.

        **2. Выбор базовой модели**
        *   Вкладка **🚀 Запуск**.
        *   **Этап обучения:** Измените на `SFT (Fine-Tuning)`.
        *   Появится поле **📦 Базовая модель**. Выберите ту модель, которую вы обучили на Шаге 1 (например, `home_pretrain/final_model`).
        *   ✨ **Магия:** Параметры архитектуры (слои, размеры) подтянутся автоматически! Вам не нужно их менять.

        **3. Настройка формата (Chat Template)**
        *   В центре экрана появится блок **🛠️ Настройка данных для SFT**.
        *   **Маппинг колонок:** Укажите, какие колонки в вашем файле отвечают за вопрос и ответ (обычно `instruction` и `output`).
        *   **Шаблон чата:** Настройте теги (например, `User:` и `Assistant:`), чтобы модель понимала, где чья реплика.
        *   Посмотрите на **👁️ Предпросмотр**, чтобы убедиться, что диалог выглядит правильно.

        **4. Запуск SFT**
        *   Нажмите **▶️ Начать тренировку**.
        *   SFT обычно требует меньше шагов (например, 500–1000).

        ---

        #### 💬 Шаг 3: Проверка
        *   Когда SFT закончится, идите на вкладку **💬 Чат**.
        *   Выберите вашу новую SFT-модель.
        *   Напишите "Привет!".
        """)

    # --- 2. КАК ЭТО РАБОТАЕТ ---
    with doc_tab2:
        st.markdown("""
        ### Что происходит внутри?
        
        **1. Нейросеть — это предсказатель следующего слова**
        Представьте, что вы читаете книгу, и я закрываю последнее слово в предложении:
        > *"Мама мыла... [?]"*
        
        Вы, скорее всего, угадаете слово *"раму"*. Нейросеть (LLM) делает то же самое. Она читает текст и пытается угадать следующее слово (токен).
        
        **2. Обучение (Training)**
        * Мы даем модели кусок текста.
        * Она делает предсказание.
        * Если она угадала неправильно, мы "наказываем" её (это называется **Loss** или Ошибка).
        * Мы чуть-чуть подкручиваем её "гайки" (веса), чтобы в следующий раз она угадала лучше.
        * Этот процесс повторяется тысячи раз.

        **3. Токенизация**
        Компьютеры не понимают слова, они понимают числа.
        Фраза *"Привет мир"* превращается в набор чисел, например `[1054, 2033]`. Эти кусочки называются **токенами**.
        """)

    # --- 3. СЛОВАРЬ ---
    with doc_tab3:
        st.markdown("""
        ### 📖 Шпаргалка по параметрам
        
        Эти страшные слова на самом деле простые:

        | Термин | Простое объяснение |
        |---|---|
        | **Loss (Лосс, Функция потерь)** | "Оценка двойка". Чем НИЖЕ это число, тем умнее модель. В начале оно большое (например, 10.0), в конце должно стать маленьким (2.0 - 3.0). |
        | **Batch Size (Батч)** | Сколько примеров текста модель читает за один раз. Чем больше — тем быстрее учится, но нужно больше памяти. |
        | **Epoch (Эпоха)** | Один полный проход по всему вашему датасету. |
        | **Learning Rate (LR)** | Скорость обучения. Если слишком быстро — модель "пролетит" знания. Если слишком медленно — будете ждать вечность. Обычно 3e-4 (0.0003) — хороший старт. |
        | **VRAM** | Видеопамять вашей видеокарты. Самый ценный ресурс. Если её не хватает, уменьшайте Batch Size. |
        | **Checkpoint (Чекпоинт)** | "Сохранение игры". Промежуточная версия модели, сохраненная на диск. |
        | **Overfitting (Переобучение)** | Когда модель просто вызубрила текст наизусть и не может придумать ничего нового. |
        """)

    # --- 4. ДАННЫЕ ---
    with doc_tab4:
        st.markdown("""
        ### Какие данные нужны?

        Модели обучаются на текстовых файлах. Мы используем формат **JSONL**.
        Это просто текстовый файл, где каждая строчка — это отдельный JSON объект.
        
        **Пример dataset.jsonl:**
        ```json
        {"text": "Александр Пушкин — великий русский поэт."}
        {"text": "Столица Франции — Париж."}
        {"text": "Рецепт блинов: мука, молоко, яйца..."}
        ```

        **Где взять данные?**
        1. **HuggingFace**: Огромная библиотека датасетов. В нашем приложении есть удобный загрузчик.
        2. **Свои файлы**: Вы можете загрузить обычный `.txt` файл (книгу, статьи), и приложение само превратит его в нужный формат.

        **Совет:** Для обучения русской модели ищите датасеты по тегу `language:ru` или используйте `HuggingFaceFW/fineweb-2` с сабсетом `rus_Cyrl`.
        """)
